#include <bits/stdc++.h>
using namespace std;

int n;
string s[30];
int cnt[30][356];

void ql(int i)
{
    if(i > n)
    {

    }
    else
    {
        for(int j = )
    }
}
main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

    cin >> n;

    for(int i = 1; i <= n; i++)
        cin >> s[i];

    for(int i = 1; i <= n; i++)
        for(int j = 1; j < s[i].size(); j++)
            cnt[s[j]]++;
    ql(0, "");
}
